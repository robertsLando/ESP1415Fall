<resources>

    
    <style name="CustomTheme"
           parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
           <item name="android:textColorPrimary">@color/orange_900</item>
           <item name="android:windowBackground">@color/orange_300</item>
    	<item name="android:colorBackground">@color/orange_100</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
           parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/orange_500</item>

        <!-- Support library compatibility -->
        <item name="background">@color/orange_500</item>
    </style>   

</resources>
